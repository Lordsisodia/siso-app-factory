# Row Level Security Policies (Placeholder)

> TODO: Document the exact Supabase RLS policies per table. Outline SELECT/INSERT/UPDATE/DELETE patterns that enforce `client_id = current_setting('app.current_client_id')` and reference helper RPCs for setting the context.

Use `database/MULTI-TENANT-SCHEMA.md` as the canonical source until this file is expanded.
